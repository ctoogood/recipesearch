import React from 'react';
import Header from './Header';

const UserLists = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};

export default UserLists;